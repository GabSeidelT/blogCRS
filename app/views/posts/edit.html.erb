<div class="container p-0 bg-gradient-light border-radius-lg">
    <div class="card card-plain">
        <div class="card-header">
            <h4 class="font-weight-bolder">Post</h4>
            <p class="mb-0">Editando o post <%= @post.name %> </p>
        </div>
        <%= render partial: 'form' %>
    </div>
</div>